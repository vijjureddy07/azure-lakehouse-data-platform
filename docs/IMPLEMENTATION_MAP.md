# Implementation Map — Skills to Implementation Matrix

> **Workflow Rule:** BUILD FIRST → DOCUMENT EVERYTHING → LEARN LATER  
> Maps every target engineering skill to its exact code implementation and output sink.

---

## 🗺️ Engineering Skill Mapping Table

| Target Skill | Data Engineering Requirement | Dataset(s) | Transformation / Logic | File / Function | Target Output Sink | Why It Exists |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **PySpark Session Config** | Low-overhead local cluster execution | System-wide | Multi-threaded local execution (`local[*]`), UTC timezone, 4 shuffle partitions | [spark.py](../src/utils/spark.py)<br>`get_spark_session()` | In-memory `SparkSession` | Avoids default 200 partition overhead and timezone serialization bugs. |
| **Explicit Schema Enforcement** | Prevent schema drift and eliminate `inferSchema` I/O | All 8 datasets | Explicit `StructType` and `StructField` contracts with `DecimalType` | [retail_schemas.py](../src/schemas/retail_schemas.py) | Raw ingestion readers | Eliminates secondary sampling file scans and runtime type inference failures. |
| **Monetary Precision** | Prevent floating-point rounding errors in financials | `products`, `orders`, `order_items`, `payments`, `returns` | `DecimalType(10, 2)` & `DecimalType(12, 2)` arithmetic | [products.py](../src/transformations/products.py), [sales.py](../src/transformations/sales.py) | `output/cleaned/` & `output/curated/` | Floating-point (Float/Double) causes precision loss in financial reporting and auditing. |
| **String Normalization & Regex** | Clean customer and product dimensions | `customers`, `products` | Whitespace trimming, email lowercase, title casing, email regex validation | [customers.py](../src/transformations/customers.py) | `output/cleaned/customers` | Inconsistent casing and malformed strings break joins and analytics. |
| **Deduplication via Window Ranking** | Eliminate duplicate primary keys deterministically | `customers`, `products`, `orders`, `payments`, `stores`, `employees` | `ROW_NUMBER() OVER (PARTITION BY pk ORDER BY timestamp DESC)` | [customers.py](../src/transformations/customers.py) | `output/cleaned/` (Valid) & `output/quarantine/` (Dups) | Preserves most recent record while isolating duplicate instances for audit. |
| **Referential Integrity Validation** | Detect orphan records across foreign keys | `orders`, `order_items`, `payments`, `employees`, `returns` | `LEFT JOIN` / `LEFT_ANTI` join to identify unreferenced parent keys | [orders.py](../src/transformations/orders.py)<br>`transform_orders()` | `output/quarantine/orders` | Prevents corrupted orphaned records from skewing dimensional joins. |
| **Quarantine Error Routing** | Standardized rejection audit trail | All datasets | Serialize raw row to JSON string, attach `rejection_reason` and timestamp | [rules.py](../src/quality/rules.py)<br>`format_as_quarantine()` | `output/quarantine/<dataset>/` | Provides zero-data-loss auditability without failing production pipelines. |
| **Multi-Table Relational Joins** | Curate denormalized sales analytics table | `orders`, `order_items`, `products`, `customers`, `stores` | Inner joins combining transactional line items with validated dimensions | [sales.py](../src/transformations/sales.py)<br>`build_curated_sales()` | `output/curated/curated_sales` | Pre-joins normalized tables for high-throughput OLAP analytics and BI. |
| **Window Functions: Ranking** | Department/Category leaderboards | `curated_sales` | `DENSE_RANK() OVER (PARTITION BY category ORDER BY total_sales DESC)` | [sales.py](../src/transformations/sales.py) | `output/curated/curated_sales` | Enables category-level top-N analysis without subqueries. |
| **Window Functions: Cumulative Spend** | Customer running spend and order sequencing | `curated_sales` | `ROW_NUMBER()`, `SUM() OVER (ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` | [sales.py](../src/transformations/sales.py) | `output/curated/curated_sales` | Computes customer order frequency and dynamic loyalty tier tracking. |
| **Window Functions: Lag Interval** | Purchase recurrence and churn analysis | `curated_sales` | `DATEDIFF(order_date, LAG(order_date, 1) OVER (PARTITION BY customer_id ORDER BY order_date))` | [sales.py](../src/transformations/sales.py) | `output/curated/curated_sales` | Measures elapsed days between transactions to detect customer churn. |
| **Spark SQL Temporary Views** | Modular ANSI SQL analytics | `v_curated_sales`, `v_returns`, `v_orders` | `createOrReplaceTempView()` with CTEs and aggregations | [local_batch_pipeline.py](../src/pipelines/local_batch_pipeline.py) & [sql/](../sql/) | Terminal reports & Spark DataFrames | Allows business analysts and BI developers to execute standard SQL queries. |
| **Temporal Partitioning in Parquet** | Optimized columnar storage layout | `curated_sales` | `partitionBy("order_year", "order_month")` | [local_batch_pipeline.py](../src/pipelines/local_batch_pipeline.py) | `output/curated/curated_sales/order_year=*/order_month=*/` | Enables partition pruning on date range filters, minimizing scanned I/O. |
| **Full Refresh Idempotency** | Repeatable local pipeline runs without duplicate build-up | All outputs | Parquet `mode("overwrite")` semantics | [local_batch_pipeline.py](../src/pipelines/local_batch_pipeline.py) | `output/` | Ensures re-executing pipeline on same input produces exact same clean state. |
| **ADLS Gen2 Hierarchical Namespace** | Atomic directory operations and POSIX structure | Cloud storage | `isHnsEnabled: true` in StorageV2 account | [main.bicep](../infra/bicep/main.bicep), [arm_template.json](../infra/arm_template.json) | ADLS Gen2 `lakehouse` container | Replaces O(N) blob copy operations with O(1) atomic directory updates for big data engines. |
| **ADF Managed Identity & RBAC** | Passwordless enterprise cloud authentication | Cloud security | SystemAssigned identity with `Storage Blob Data Contributor` | [main.bicep](../infra/bicep/main.bicep), [ls_adls_gen2.json](../adf/linkedService/ls_adls_gen2.json) | Azure Data Factory -> ADLS Gen2 | Eliminates hardcoded access keys and connection strings from Git and configuration files. |
| **ADF Parameterized Linked Services & Datasets** | Reusable connection and file pointers | All 8 datasets | Binary datasets parameterized with `@dataset().*` expressions | [ds_http_raw_file.json](../adf/dataset/ds_http_raw_file.json), [ds_adls_landing_file.json](../adf/dataset/ds_adls_landing_file.json) | ADF Control Plane | Decouples physical file paths from pipeline logic, enabling generic reusable data movement. |
| **ADF Master-Child Metadata Orchestration** | Scalable batch file ingestion | All 8 datasets | `ForEach` activity iterating over `datasets_config` calling `pl_ingest_single_file` | [pl_master_retail_ingestion.json](../adf/pipeline/pl_master_retail_ingestion.json) | ADF Pipeline Runs | Onboards new datasets via metadata configuration without altering visual pipeline canvases. |
| **Dynamic Auditable Landing Paths** | Date-partitioned immutable raw snapshots | All 8 datasets | `@concat('landing/retail/', item().dataset_name, '/ingestion_date=', formatDateTime(utcnow(), 'yyyy-MM-dd'), '/run_id=', pipeline().RunId)` | [pl_master_retail_ingestion.json](../adf/pipeline/pl_master_retail_ingestion.json) | `landing/retail/<dataset>/ingestion_date=*/run_id=*/*` | Guarantees immutable audit trail and historical raw file traceability per ADF execution run. |
| **Incremental Landing Discovery & Audit Tracking** | Prevent duplicate ingestion across ADF landing batches | All 8 datasets | Regex path discovery + Delta `_ingestion_audit` tracking table | [discovery.py](../src/medallion/discovery.py)<br>`filter_uningested_files()` | `output/delta/bronze/_ingestion_audit` | Guarantees incremental-safe Bronze processing across arbitrary pipeline run IDs. |
| **Bronze Delta Ingestion with Lineage** | Ingest raw files with system audit columns | All 8 datasets | Preserve raw strings + append `_source_file`, `_source_path`, `_ingestion_date`, `_adf_run_id`, `_ingested_timestamp` | [bronze.py](../src/medallion/bronze.py)<br>`ingest_bronze_layer()` | `output/delta/bronze/<dataset>/` | Preserves raw fidelity with end-to-end cloud lineage. |
| **Silver Conformed & Quality Quarantine** | Typed schemas, Decimal precision, and defect routing | All 8 datasets | Strong casting, email regex, duplicate ranking to quarantine, FK anti-joins | [silver.py](../src/medallion/silver.py)<br>`process_silver_layer()` | `output/delta/silver/<dataset>/` & `output/delta/silver/quarantine/<dataset>/` | Enforces mathematical reconciliation (`bronze == valid + quarantine`) without silent data loss. |
| **Idempotent Delta MERGE (Upsert)** | Key-matched dimensional updates | `customers`, `products`, `orders` | `DeltaTable.merge()` on primary key with `whenMatchedUpdateAll()` and `whenNotMatchedInsertAll()` | [merge.py](../src/medallion/merge.py)<br>`upsert_delta_table()` | `output/delta/silver/<dataset>/` | Provides ACID row-level upserts with rerun idempotency. |
| **Gold Business KPI Delta Aggregations** | High-performance analytical aggregates | 6 Business Views | Daily sales, monthly revenue, store region, category performance, customer spending, return root cause | [gold.py](../src/medallion/gold.py)<br>`process_gold_layer()` | `output/delta/gold/<table_name>/` | Serves business BI and reporting directly from conformed Silver data. |
| **Delta Transaction Log & Time Travel** | ACID atomicity, history auditing, and version snapshotting | Delta tables | `_delta_log/*.json`, `DESCRIBE HISTORY`, `versionAsOf` queries | [features.py](../src/delta/features.py)<br>`query_time_travel_by_version()` | Delta Table Versions | Enables point-in-time auditing and rollbacks. |
| **Schema Enforcement & Evolution** | Data governance against unintended schema drift | Delta tables | Default schema validation on append + `.option("mergeSchema", "true")` | [features.py](../src/delta/features.py)<br>`demonstrate_schema_enforcement()` | Delta Table Schemas | Prevents silent data corruption while supporting governed schema evolution. |
| **Unity Catalog Governance & Access Connector** | Zero-secret cloud governance | Databricks / ADLS Gen2 | 3-level namespace (`catalog.schema.table`), Access Connector Managed Identity, Storage Credentials, External Locations | [main.bicep](../infra/bicep/main.bicep), [01_unity_catalog_setup.sql](../databricks/sql/01_unity_catalog_setup.sql) | Unity Catalog Metastore | Replaces legacy cluster credentials and DBFS mounts with enterprise Entra ID RBAC. |
| **Deterministic Surrogate Keys** | Compact integer surrogate key allocation in distributed Spark | `dim_customer`, `dim_product`, `dim_store`, `dim_employee`, `dim_date` | `max_existing_key + ROW_NUMBER() OVER (ORDER BY natural_key)` | [surrogate_keys.py](../src/modeling/surrogate_keys.py)<br>`assign_surrogate_keys()` | `delta/warehouse/dim_*` | Avoids gaps and non-deterministic IDs from `monotonically_increasing_id()`. |
| **Calendar Dimension Generation** | Standardized enterprise date dimension | `dim_date` | Date sequence generation (2020-2030) with yyyyMMdd integer keys, calendar attributes, and unknown member `0` | [dimensions.py](../src/modeling/dimensions.py)<br>`build_dim_date()` | `delta/warehouse/dim_date` | Powers temporal rollups, fiscal calendar slicing, and standardizes date keys. |
| **SCD Type 1 Dimension Processing** | Overwrite dimension attributes in place | `dim_product` | Delta MERGE matching on `product_id` updating attributes while preserving stable `product_key` | [scd_type1.py](../src/modeling/scd_type1.py)<br>`process_dim_product_scd1()` | `delta/warehouse/dim_product` | Keeps dimension up-to-date without preserving historical changes for non-critical attributes. |
| **SCD Type 2 Dimension Processing** | Track historical dimension changes with validity intervals | `dim_customer` | SHA-256 attribute hash, left join on active records, Delta MERGE expiration (`effective_to = now, is_current = false`), and versioned append | [scd_type2.py](../src/modeling/scd_type2.py)<br>`process_dim_customer_scd2()` | `delta/warehouse/dim_customer` | Preserves point-in-time historical accuracy for customer loyalty tiers and geographic moves. |
| **Point-in-Time Fact Resolution** | Accurate temporal dimension key lookup | `fact_sales`, `fact_returns` | Join orders to `dim_customer` on `customer_id` where `order_timestamp >= effective_from AND (order_timestamp < effective_to OR effective_to IS NULL)` | [facts.py](../src/modeling/facts.py)<br>`build_fact_sales_dataframe()` | `delta/warehouse/fact_sales` | Prevents inaccurate historical revenue misattribution to future customer attribute states. |
| **Fact Grain & Financial Precision** | One row per transaction line-item / return event | `fact_sales`, `fact_returns` | Exact `Decimal(10, 2)` calculations: `gross_amount`, `discount_amount`, `net_amount`, `cost_amount`, `profit_amount` | [facts.py](../src/modeling/facts.py)<br>`process_fact_sales()` | `delta/warehouse/fact_sales` | Enables granular line-item OLAP analysis without floating-point inaccuracies. |
| **Enterprise Data Quality Gates** | Automated pre-serving validation and hard-fail gate | Warehouse Dimensions & Facts | Completeness, Uniqueness, Referential Integrity, SCD2 Temporal Invariants, Measure Validity, Reconciliation | [quality.py](../src/modeling/quality.py)<br>`run_warehouse_quality_suite()` | `delta/warehouse/quality_audit` | Halts pipeline via `WarehouseQualityGateError` on critical violations, preventing dirty data publishing. |
| **Warehouse Sales Reconciliation** | Guarantee 100% row count and currency balance | `silver/order_items` vs `warehouse/fact_sales` | Exact row-count match and Decimal subtraction difference validation | [reconciliation.py](../src/modeling/reconciliation.py)<br>`reconcile_warehouse_sales()` | Audit log & stdout | Mathematically proves zero data loss and financial integrity between Silver and Warehouse layers. |

